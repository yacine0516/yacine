{
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "$MapboxAccessToken"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
